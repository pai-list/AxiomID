--- src/app/api/sandbox/dev-token/route.ts
+++ src/app/api/sandbox/dev-token/route.ts
@@ -1,5 +1,6 @@
 import { NextRequest } from "next/server";
 import { apiSuccess, apiError } from "@/lib/errors";
+import { logger } from "@/lib/logger";

 export async function POST(req: NextRequest) {
   // Sandbox env-only check (prevent production abuse)
@@ -10,6 +11,7 @@
     return apiSuccess({ token: "sandbox-token" }, 200);
   } catch (error: any) {
+    logger.error("Failed to generate dev token", { error });
     return apiError(500, "Failed to generate token", error.message);
   }
 }
