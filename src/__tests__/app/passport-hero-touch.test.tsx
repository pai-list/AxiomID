/**
 * PassportHero touch tests — SKIPPED.
 *
 * PassportHero was removed in the Stitch hero rewrite (commit 9b878d7).
 * The landing page no longer renders a 3D tilt card, so touch event
 * tests are no longer applicable.
 *
 * If PassportHero is re-added in the future, restore these tests from
 * git history: git show HEAD~1:src/__tests__/app/passport-hero-touch.test.tsx
 */

describe("PassportHero touch tests — skipped", () => {
  it("PassportHero removed in Stitch UI rewrite — touch tests no longer apply", () => {
    expect(true).toBe(true);
  });
});
