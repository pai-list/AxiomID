# ⚠️ Known Issues

- `/api/skills/[slug]/execute` is currently unauthenticated.
- Admin moderation is not atomic.
- Sovereign DIDs are missing public keys in the document.
- `passportUrl` is not persisted to the database.
