# Workflow Index — AxiomID

Version: 1.0
Generated: 2026-07-13
Generated by: Agent Beta
Confidence: 97%
Sources:
- .github/workflows/*.yml (12 files)
- .coderabbit.yaml
- renovate.json
- .github/workflows/HERMES_WEEKLY_GOALS.md
- .github/workflows/PER_PR_LOOP.md
- next.config.ts
- vercel.json
Last Verified: 2026-07-13

---

## CI/CD Workflows

### 1. ci.yml — Main CI Pipeline

| Property | Value |
|---|---|
| **File** | `.github/workflows/ci.yml` |
| **Trigger** | Push to main, PR to main |
| **Concurrency** | Cancel in-progress per ref |
| **Purpose** | TypeScript type-check + ESLint lint + Jest tests + Next.js build + knip dead-code check |

**Jobs:**
| Job | Runs On | Steps | Status |
|---|---|---|---|
| `verify` | ubuntu-latest | Checkout → Setup Node 22 → npm ci → prisma generate → type-check → lint → build → test --ci → knip | ✅ Active |

**Evidence:** `.github/workflows/ci.yml:1-51`
**Last run:** Active (triggered per commit)

---

### 2. codeql.yml — CodeQL Security Analysis

| Property | Value |
|---|---|
| **File** | `.github/workflows/codeql.yml` |
| **Trigger** | Push to main, PR to main, schedule (Mon 6AM UTC) |
| **Purpose** | GitHub CodeQL security vulnerability scanning |

**Jobs:**
| Job | Language | Build Mode | Status |
|---|---|---|---|
| `analyze` | javascript-typescript | none | ✅ Active |

**Evidence:** `.github/workflows/codeql.yml:1-46`
**Notes:** Uses `actions/checkout@v7`, `github/codeql-action/init@v4`, `github/codeql-action/analyze@v4`. Persist-credentials disabled. 14 of 19 vulnerabilities resolved in PR #291; ReDoS fixed in ast-scanner.ts; 8 false positives triaged.

---

### 3. label.yml — Expert Auto-Labeler

| Property | Value |
|---|---|
| **File** | `.github/workflows/label.yml` |
| **Trigger** | pull_request_target |
| **Purpose** | 7-stage labeling: path-based area → size → conventional commit type → AI-author detection → breaking change → critical path human approval → merge conflict |

**Stages:**
| Stage | Method | Status |
|---|---|---|
| 1. Path-based area labels | `actions/labeler@v6` | ✅ Active |
| 2. Size detection (XS→XL) | `actions/github-script@v9` | ✅ Active |
| 3. Conventional commit type | `actions/github-script@v9` | ✅ Active |
| 4. AI author detection | `actions/github-script@v9` | ✅ Active |
| 5. Breaking change detection | `actions/github-script@v9` | ✅ Active |
| 6. Critical path human approval | `actions/github-script@v9` | ✅ Active |
| 7. Merge conflict detection | `actions/github-script@v9` | ✅ Active |

**Critical Patterns:** auth middleware, errors.ts, pi-sdk, oauth2, rate-limiter, revocation, prisma/schema.prisma, backend auth, packages/crypto

**Evidence:** `.github/workflows/label.yml:1-256`, PROJECT_STATUS.md:40-42 (PR #288)

---

### 4. ai-pr-health.yml — AI PR Health Check

| Property | Value |
|---|---|
| **File** | `.github/workflows/ai-pr-health.yml` |
| **Trigger** | PR (opened, synchronize, reopened) |
| **Purpose** | PR hygiene: cross-boundary security detection, large PR flagging, auth changes without tests, size labeling |

**Checks:**
| Check | Threshold | Action |
|---|---|---|
| Cross-boundary security touch | Security files + >3 non-security files | `needs-human-approval` + `priority:high` |
| Large PR | >500 lines | Flagged in output |
| Auth changes without tests | Auth touched, tests not touched | `needs-human-approval` |
| Size labeling | ≤10/≤50/≤200/≤1000/1000+ | XS/S/M/L/XL |

**Evidence:** `.github/workflows/ai-pr-health.yml:1-133`

---

### 5. gemini-review.yml — Gemini Code Review

| Property | Value |
|---|---|
| **File** | `.github/workflows/gemini-review.yml` |
| **Trigger** | PR (opened, synchronize, reopened) |
| **Purpose** | AI code review using Groq (llama-3.3-70b-versatile), posts findings as PR comment |

**Method:**
1. Get PR diff via `gh pr diff`
2. Send diff to Groq API for analysis
3. Parse JSON findings (severity/file/issue/suggestion)
4. Post structured comment to PR

**Secrets required:** `GROQ_API_KEY`

**Evidence:** `.github/workflows/gemini-review.yml:1-140`

---

### 6. soul-check.yml — SOUL Alignment Check

| Property | Value |
|---|---|
| **File** | `.github/workflows/soul-check.yml` |
| **Trigger** | PR (opened, synchronize, reopened) |
| **Purpose** | Enforce AGENTS.md compliance: religious name leaks in code, auth changes without matching tests, t() helper misuse |

**Rules:**
| Rule | Detection | Action |
|---|---|---|
| Religious name leaks | Banned Arabic terms in code (not docs) | PR comment warning |
| Auth changes without tests | auth/ dir modified, __tests__/ not modified | PR comment warning |
| t() helper misuse | t() called with 2+ arguments | PR comment warning |

**Evidence:** `.github/workflows/soul-check.yml:1-122`

---

### 7. ci-intelligence.yml — CI Intelligence Agent

| Property | Value |
|---|---|
| **File** | `.github/workflows/ci-intelligence.yml` |
| **Trigger** | PR (opened, synchronize, reopened) |
| **Purpose** | Index PR code changes to Cloudflare Vectorize for cross-PR semantic search + anomaly detection |

**Steps:**
1. Get PR diff
2. Extract per-file metadata (additions, deletions, extension)
3. Detect anomalies (large PR, security-sensitive files, large scope)
4. Embed file metadata via Workers AI (bge-base-en-v1.5)
5. Insert vectors into Vectorize index `ci-code-vectors`

**Secrets required:** `CF_ACCOUNT_ID`, `CF_API_TOKEN`

**Evidence:** `.github/workflows/ci-intelligence.yml:1-167`

---

### 8. ai-issue-triage.yml — AI Issue Triage

| Property | Value |
|---|---|
| **File** | `.github/workflows/ai-issue-triage.yml` |
| **Trigger** | Issue (opened, reopened) |
| **Purpose** | Auto-classify new issues with labels via Groq LLM, post AI summary comment |

**Label Categories:**
| Category | Labels |
|---|---|
| Type | bug, enhancement, feat, question, needs-triage |
| Priority | critical, high, medium, low |
| Area | pi-network, did-passport, frontend, cloudflare-workers, database, documentation, ci/cd, infra |

**Secrets required:** `GROQ_API_KEY`

**Evidence:** `.github/workflows/ai-issue-triage.yml:1-160`

---

### 9. loops.yml — Continuous Improvement Loops

| Property | Value |
|---|---|
| **File** | `.github/workflows/loops.yml` |
| **Trigger** | Nightly (2AM UTC) + manual dispatch (`workflow_dispatch`) |
| **Purpose** | 5 quality loops: sub-50ms page load, test coverage, logging coverage, fresh clone, nightly changelog |

**Loops:**
| Loop | Purpose | Status |
|---|---|---|
| sub-50ms | Measure page load times for key routes | ✅ Active |
| coverage | Run Jest coverage, enforce >80% threshold | ✅ Active |
| logging-coverage | Check all API routes have logger.error() in catch | ✅ Active |
| fresh-clone | Test fresh clone onboarding (ci → build → test) | ✅ Active |
| nightly-changelog | Collect daily commits, update CHANGELOG.md | ✅ Active* |

**Permissions:** `contents: write`, `pull-requests: write`

**Evidence:** `.github/workflows/loops.yml:1-176`, AGENTS.md:197-216

---

### 10. npm-publish.yml — npm Package Publishing

| Property | Value |
|---|---|
| **File** | `.github/workflows/npm-publish.yml` |
| **Trigger** | Tag push `v*` |
| **Purpose** | Build and publish @axiomid/crypto + @axiomid/sdk to npm registry |

**Steps:**
| Step | Detail |
|---|---|
| Build @axiomid/crypto | `cd packages/crypto && npm run build` |
| Build @axiomid/sdk | `cd packages/sdk && npm run build` |
| Publish @axiomid/crypto | `npm publish --access public --provenance` |
| Publish @axiomid/sdk | `npm publish --access public --provenance` |

**Secrets required:** `NPM_TOKEN`
**Permissions:** `id-token: write` (for OIDC provenance)

**Evidence:** `.github/workflows/npm-publish.yml:1-39`, CHANGELOG.md:86-87, PROJECT_STATUS.md:64-66 (PR #293)

---

### 11. skill-quality.yml — Skill Quality Gate

| Property | Value |
|---|---|
| **File** | `.github/workflows/skill-quality.yml` |
| **Trigger** | PR targeting `src/data/skills.json` or `prisma/**` |
| **Purpose** | Validate skill manifest integrity on changes |

**Steps:**
1. `npm ci --ignore-scripts`
2. `npx tsx scripts/validate-skill-manifest.ts --changed`

**Evidence:** `.github/workflows/skill-quality.yml:1-21`

---

### 12. hermes-weekly.yml — Hermes Weekly Loop

| Property | Value |
|---|---|
| **File** | `.github/workflows/hermes-weekly.yml` |
| **Trigger** | Schedule (Sun 10AM UTC) + manual dispatch |
| **Purpose** | Collect weekly metrics and generate synthesized status report |

**Metrics Collected:**
| Metric | Source |
|---|---|
| Commits this week | `git log --since=7-days` |
| Merged PRs | `gh pr list --state merged` |
| Open PRs | `gh pr list --state open` |
| Open Issues | `gh issue list --state open` |
| Latest CI status | `gh run list --workflow=ci.yml` |
| Stale branches | `git branch -r --no-merged main` |

**Evidence:** `.github/workflows/hermes-weekly.yml:1-130`

---

### 13. telegram-notify.yml — Telegram Notifications

| Property | Value |
|---|---|
| **File** | `.github/workflows/telegram-notify.yml` |
| **Trigger** | PR (opened, closed, reopened), workflow_run (ci, CI Intelligence, Gemini Review) |
| **Purpose** | Send real-time notifications to Telegram chat |

**Events:**
| Event | Message Format |
|---|---|
| PR opened | 🆕 PR opened: title by @author |
| PR merged | ✅ PR merged: title |
| PR closed (unmerged) | ❌ PR closed: title |
| PR reopened | 🔄 PR reopened: title |
| Workflow success | ✅ Workflow: name |
| Workflow failure | ❌ Workflow: name |

**Secrets required:** `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`

**Evidence:** `.github/workflows/telegram-notify.yml:1-94`

---

## External Integrations

### CodeRabbit (AI Code Review)

| Property | Value |
|---|---|
| **Config** | `.coderabbit.yaml` |
| **Trigger** | PR events |
| **Purpose** | Automated PR code review with 8 validated learning patterns |
| **Status** | ⚠️ Credits exhausted (billing issue), rate limited |

**Evidence:** `.coderabbit.yaml`, PROJECT_STATUS.md, AGENTS.md (CodeRabbit Learning Patterns section)

### Renovate (Dependency Updates)

| Property | Value |
|---|---|
| **Config** | `renovate.json` |
| **Trigger** | Schedule |
| **Purpose** | Automated dependency update PRs |
| **Status** | ✅ Active |

**Evidence:** `renovate.json`

### Vercel Deploy (CD)

| Property | Value |
|---|---|
| **Config** | `vercel.json`, project settings |
| **Trigger** | Push to main (production), PR (preview) |
| **Purpose** | Deploy Next.js app to production/preview |
| **Status** | ✅ Active |
| **Production URL** | https://axiomid.app |

**Evidence:** `vercel.json`, Cognitive OS summary (session 2026-07-11)

---

## Workflow Dependency Graph

```
PR Opened
  ├─ ci.yml (type-check, lint, test, build)
  ├─ codeql.yml (security scan)
  ├─ label.yml (7-stage labeling)
  ├─ ai-pr-health.yml (hygiene checks)
  ├─ gemini-review.yml (AI review via Groq)
  ├─ soul-check.yml (SOUL alignment)
  ├─ ci-intelligence.yml (Vectorize indexing)
  └─ telegram-notify.yml (notification)

PR Merged to main
  ├─ ci.yml (verification)
  ├─ codeql.yml (security scan)
  ├─ Vercel (production deploy)
  └─ telegram-notify.yml (notification)

Tag v*
  └─ npm-publish.yml (publish packages)

Nightly (2AM UTC)
  └─ loops.yml (quality loops)

Weekly (Sun 10AM UTC)
  └─ hermes-weekly.yml (status report)

Issue Created
  └─ ai-issue-triage.yml (classification)
```

---

## Summary

| Category | Count | Workflows |
|---|---|---|
| **CI (push/PR)** | 2 | ci.yml, codeql.yml |
| **PR Intelligence** | 4 | label.yml, ai-pr-health.yml, gemini-review.yml, soul-check.yml |
| **AI Automation** | 2 | ci-intelligence.yml, ai-issue-triage.yml |
| **Quality** | 2 | loops.yml, skill-quality.yml |
| **Release** | 1 | npm-publish.yml |
| **Reporting** | 1 | hermes-weekly.yml |
| **Notifications** | 1 | telegram-notify.yml |
| **Total** | **13** | |
| **External** | 3 | CodeRabbit, Renovate, Vercel Deploy |
