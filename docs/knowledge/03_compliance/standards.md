# Standards Compliance Audit

Version: 1.0
Generated: 2026-07-13
Generated by: Agent Charlie (Phase 2)
Confidence: 91%
Sources:
- src/lib/did.ts
- src/lib/did-document.ts
- src/lib/vc.ts
- src/lib/pi-signin.ts
- src/lib/oauth-state.ts
- src/app/.well-known/oauth-authorization-server/route.ts
- src/app/.well-known/oauth-protected-resource/route.ts
- src/app/api/route.ts
- src/middleware.ts
- src/types/global.d.ts
- backend/src/mcp/server.ts
- backend/src/router.ts
- prisma/schema.prisma
- AGENTS.md
Last Verified: 2026-07-13

---

## Standards Compliance Matrix

| Standard | Supported | Partial | Missing | Planned | Evidence |
|----------|-----------|---------|---------|---------|----------|
| **W3C DID Core 1.0** | ✅ | — | — | — | `did:axiom` method implemented at `src/lib/did.ts:5-28`. DID document builder at `src/lib/did-document.ts:73-98`. DID resolution at `/.well-known/did.json` rewrite (`src/middleware.ts:78-81`). MCP tool `did_resolve` at `backend/src/mcp/server.ts:28-59`. |
| **W3C DID Resolution** | — | ✅ | Full DID Resolution spec not implemented | — | `/.well-known/did.json` serves static DID document. MCP `did_resolve` queries D1 for harvest activity. No DID Resolution v1.0 `resolve()` / `resolveRepresentation()` endpoints. |
| **W3C Verifiable Credentials (VC)** | ✅ | — | — | — | `signCredential()` at `src/lib/vc.ts:20-58` with Ed25519 + RS256 support. `W3C_CONTEXT` URLs point to `https://www.w3.org/2018/credentials/v1` and `https://w3id.org/security/suites/ed25519-2020/v1`. Social, Passport, Agent credentials implemented. |
| **VC Data Model 1.1** | ✅ | — | — | — | Uses `@context`, `id`, `type`, `issuer`, `issuanceDate`, `credentialSubject`, `proof` fields. `proof` uses `Ed25519Signature2020` and `RsaSignature2018` types. |
| **VC Presentation** | — | — | ❌ Not implemented | — | No `VerifiablePresentation` wrapper. No presentation request/response flow. |
| **JSON-LD** | — | ✅ | JSON-LD context URLs used in VC (`src/lib/vc.ts:7-10`). DID document uses `https://www.w3.org/ns/did/v1` (`src/lib/did-document.ts:4`). | No JSON-LD processing library. Contexts are static URLs, not actively resolved. | P3 |
| **OIDC (OpenID Connect)** | — | — | ❌ Not implemented | — | OAuth authorization server advertises `agent_auth` type as `"oidc"` (`src/app/.well-known/oauth-authorization-server/route.ts:22-26`) but no actual OIDC `id_token` issuance. Claims `id_token_type: "urn:ietf:params:oauth:token-type:id_token"` but no id_token generated. |
| **OAuth 2.0** | — | ✅ | — | — | Authorization server metadata at `/.well-known/oauth-authorization-server`. Token endpoint at `/api/oauth2/token`. Revocation at `/api/oauth2/revoke`. Agent auth uses custom grant type `"claim"` + `"urn:ietf:params:oauth:grant-type:jwt-bearer"`. |
| **OAuth 2.0 State Protection** | ✅ | — | — | — | `verifyState()` in `src/lib/oauth-state.ts` (tested at `src/__tests__/lib/oauth-state.test.ts`). State stored in sessionStorage via `initiatePiSignIn()`. |
| **MCP (Model Context Protocol)** | ✅ | — | — | — | Full MCP server at `backend/src/mcp/server.ts:1-390` with 8 tools: `did_resolve`, `did_create`, `trust_score`, `trust_delegate`, `trust_chain`, `presence_heartbeat`, `presence_status`, `skill_list`, `skill_install`, `harvest_query`, `harvest_result`. Handler at `backend/src/mcp/handler.ts`. Uses `@modelcontextprotocol/sdk/server/mcp.js`. |
| **.well-known discovery** | ✅ | — | — | — | `/.well-known/did.json` (rewritten to `/api/did-document`), `/.well-known/oauth-authorization-server`, `/.well-known/oauth-protected-resource`, `/.well-known/jwks.json` |
| **JWK / JWKS** | — | ✅ | — | — | JWKS URI published at `https://axiomid.app/.well-known/jwks.json` (referenced in `src/app/api/route.ts:25`). Implementation not audited (separate route). |

---

## DID Core Compliance

### Supported Features (`src/lib/did.ts` + `src/lib/did-document.ts`)

| W3C DID Core Method | Implemented | Location |
|---------------------|-------------|----------|
| DID Syntax (`did:axiom:pi:{id}`) | ✅ | `src/lib/did.ts:11` |
| DID Creation | ✅ | `createUserDid()`, `createPiDid()`, `createIssuerDid()` |
| DID Document Generation | ✅ | `buildDidDocument()` at `src/lib/did-document.ts:73-98` |
| Verification Method (`Ed25519VerificationKey2020`) | ✅ | `src/lib/did-document.ts:87-92` |
| Authentication (key reference) | ✅ | `src/lib/did-document.ts:93` |
| Assertion Method (key reference) | ✅ | `src/lib/did-document.ts:94` |
| Public Key Multibase encoding | ✅ | `pemToMultibase()` at `src/lib/did-document.ts:36-48` |

### Missing DID Features

| Feature | Status | Notes |
|---------|--------|-------|
| DID Resolution v1.0 | ❌ | No `GET /{did}` resolution endpoint per W3C spec |
| DID URL Dereferencing | ❌ | No `GET /{did}/{path}` dereferencing |
| Service Endpoints | ❌ | No `service` array in DID documents |
| Key Rotation | ❌ | No `keyAgreement`, `capabilityInvocation`, `capabilityDelegation` |
| DID Subject | ❌ | `id` is always the DID itself — no `alsoKnownAs` |
| DID Method Metadata | ❌ | No method-specific properties |
| DID Resolution Metadata | ❌ | No `didResolutionMetadata` in response |

---

## VC Compliance

### Supported Features (`src/lib/vc.ts`)

| W3C VC Feature | Implemented | Location |
|----------------|-------------|----------|
| `@context` (W3C VC v1) | ✅ | `src/lib/vc.ts:7-9` |
| `id` (credential ID) | ✅ | `src/lib/vc.ts:81` |
| `type` array | ✅ | `src/lib/vc.ts:82` |
| `issuer` (DID) | ✅ | `src/lib/vc.ts:83` |
| `issuanceDate` (ISO 8601) | ✅ | `src/lib/vc.ts:84` |
| `credentialSubject` | ✅ | `src/lib/vc.ts:85` |
| `proof.type` | ✅ | `Ed25519Signature2020`, `RsaSignature2018` |
| `proof.created` | ✅ | ISO 8601 timestamp |
| `proof.verificationMethod` | ✅ | `{issuer}#key-1` |
| `proof.proofPurpose` | ✅ | `assertionMethod` |
| `proof.proofValue` | ✅ | Raw signature (hex string) |
| Credential types | ✅ | `SocialIdentityCredential`, `AxiomPassportCredential`, `AxiomAgentAttestationCredential` |

### Missing VC Features

| Feature | Status | Notes |
|---------|--------|-------|
| Verifiable Presentation | ❌ | No presentation wrapper |
| VC Status (revocation) | ❌ | No `credentialStatus` field |
| Terms of Use | ❌ | No `termsOfUse` field |
| Evidence | ❌ | No `evidence` field |
| Refresh Service | ❌ | No `refreshService` |
| Expiration Date | ❌ | No `expirationDate` — credentials are perpetual |
| Zero-Knowledge Proofs | ❌ | Not supported (only Ed25519 + RSA) |
| BBS+ Signatures | ❌ | Not supported |

---

## OAuth 2.0 / OIDC Compliance

### Supported Features

| Feature | Status | Evidence |
|---------|--------|----------|
| Authorization Server Metadata | ✅ | `/.well-known/oauth-authorization-server` at `src/app/.well-known/oauth-authorization-server/route.ts` |
| Token Endpoint | ✅ | `/api/oauth2/token` (declared in `src/app/api/route.ts:20`) |
| Revocation Endpoint | ✅ | `/api/oauth2/revoke` (declared in `src/app/api/route.ts:21`) |
| Grant Types | ✅ | `jwt-bearer` + custom `claim` |
| Scopes | ✅ | `api.read`, `api.write`, `agent.sign` |
| State Parameter | ✅ | `pi_oauth_state` verification in `src/lib/oauth-state.ts` |
| Pi Sign-In | ✅ | `initiatePiSignIn()` with OAuth redirect to `https://accounts.pinet.com/oauth/authorize` |

### Missing OAuth/OIDC Features

| Feature | Status | Notes |
|---------|--------|-------|
| Authorization Code Flow | ❌ | Uses implicit `response_type=token` or custom `agent_auth` |
| PKCE | ❌ | No code challenge/verifier |
| ID Token (OIDC) | ❌ | Declared in metadata but no actual `id_token` |
| UserInfo Endpoint | ❌ | No `/.well-known/openid-configuration` or `/userinfo` |
| Refresh Token | ❌ | No `refresh_token` grant type |
| Client Registration | ❌ | No dynamic client registration |
| Scope validation | ❌ | No scope-to-permission mapping |

---

## MCP (Model Context Protocol) Compliance

### Supported Features (`backend/src/mcp/server.ts`)

| Feature | Status | Details |
|---------|--------|---------|
| MCP Server | ✅ | Implemented using `@modelcontextprotocol/sdk` |
| Tool Definitions | ✅ | 10 tools with Zod schemas |
| Tool Arguments | ✅ | Each tool has typed, described arguments |
| Tool Results | ✅ | Structured `{ content: [{ type, text }] }` responses |
| Error Handling | ✅ | `isError: true` returned on failures |
| DID Resolution Tool | ✅ | `did_resolve` — queries D1 for user activity |
| DID Creation Tool | ✅ | `did_create` — creates new DID with initial trust |
| Trust Tools | ✅ | `trust_score`, `trust_delegate`, `trust_chain` |
| Presence Tools | ✅ | `presence_heartbeat`, `presence_status` |
| Skill Tools | ✅ | `skill_list`, `skill_install` |
| Harvest Tools | ✅ | `harvest_query`, `harvest_result` |

### Missing MCP Features

| Feature | Status | Notes |
|---------|--------|-------|
| MCP Resource (readable data) | ❌ | Not implemented — tools only |
| MCP Prompt | ❌ | Not implemented |
| MCP Sampling | ❌ | Not implemented |
| MCP Roots | ❌ | Not implemented |
| MCP Transport (SSE) | ❌ | Currently uses stdio/WebSocket? Transport not defined in code |
| MCP Client-side SDK | ❌ | No client SDK to consume these tools from IDE |
| MCP Agent-to-Agent | ❌ | No inter-agent MCP protocol |

---

## Recommended Actions

1. **P1:** Implement VC `expirationDate` field — perpetual credentials are a security risk
2. **P1:** Add DID Resolution v1.0 endpoint (`GET /1.0/identifiers/{did}`)
3. **P2:** Add Verifiable Presentation wrapper for credential sharing
4. **P2:** Implement actual OIDC `id_token` issuance (authorization server metadata claims it)
5. **P2:** Add PKCE to OAuth flow for secure mobile auth
6. **P3:** Add `service` endpoints to DID documents (point to agent endpoints, MCP server)
7. **P3:** Implement VC status/revocation mechanism via `credentialStatus`
8. **P3:** Add MCP resource definitions for user trust data
