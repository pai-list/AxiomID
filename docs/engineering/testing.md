# 🧪 Testing: Verification-First

## Standards
- **TDD**: Write the failing test before the implementation.
- **No Mock-Slippage**: Ensure tests simulate the Pi Browser environment (`User-Agent` headers).
- **Coverage**: Target 100% coverage for all core `lib/` and `api/` routes.
- **Standard Matchers**: Use only standard Jest matchers.
